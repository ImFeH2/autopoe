[package]
name = "agent"
version = "0.0.1"
edition = "2024"

[dependencies]
futures-util = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
futures-executor = "0.3"
